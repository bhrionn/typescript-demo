/**
 * useAuth Hook
 * Re-exports the useAuth hook from AuthContext for convenience
 * This allows importing from hooks/ directory following project structure
 */

export { useAuth } from '../contexts/AuthContext';
